# Foro Simple con Docker

Foro básico con autenticación usando Flask, SQLite, Docker y Jenkins.

## 🚀 Tecnologías

- Backend: Flask (Python)
- Base de datos: SQLite
- Contenedores: Docker
- CI/CD: Jenkins
- Control de versiones: Git

## 📦 Instalación

```bash
docker-compose up -d --build
